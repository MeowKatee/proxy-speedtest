# proxy-speedtest

Universal proxy lantancy, single-threaded download speed test tool, with sing-box backended,

based on [`singbox-dae-bridge`](https://github.com/MeowKatee/singbox-dae-bridge/), which generates socks5 proxy inbound.

## Limitation

You cannot test over thousands of proxies, due to port number limitation.

## Usage

Check `-h`/`--help` output.
